runtime: nodejs # or nodejs10

# flexible environment
env: flex
manual_scaling:
        instances: 1
resources:
        cpu: 1
        memory_gb: 0.5
        disk_size_gb: 10
network:
        session_affinity: true
